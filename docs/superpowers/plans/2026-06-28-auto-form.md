# auto-form — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create the `@code2-base-ui/auto-form` monorepo package ecosystem — a framework for generating forms from schemas (Zod, TypeBox) with interchangeable form state adapters (TanStack Form, React Hook Form) and UI renderers (shadcn).

**Architecture:** Multi-package structure within the monorepo. Core (`auto-form`) is a lightweight factory + types + context. Providers (`auto-form-provider-zod`), adapters (`auto-form-adapter-tanstack`), renderers (`auto-form-render-shadcn`) are separate peer-dependency-free packages. Presets (`preset/auto-form-tanstack-shadcn`) compose them. Distribution via GitHub Registry.

**Tech Stack:** TypeScript 6, React 19, vitest, `json-schema-toolkit` (FieldRegistry, SchemaAdapter, flatfields), Zod, `@tanstack/react-form`, shadcn/ui (Base UI primitives).

## Global Constraints

- `verbatimModuleSyntax: true` — always `import type` for types
- `noUncheckedIndexedAccess: true` — array/index access always `| undefined`
- Indentation: tabs
- Quotes: double
- Type-first TDD: write `expectTypeOf` tests before any runtime implementation
- Imports from `json-schema-toolkit`: `FieldRegistry` from `@code2-base-ui/json-schema-toolkit/registry`, types from `@code2-base-ui/json-schema-toolkit`
- React 19, TypeScript 6
- Packages are private (monorepo), `"type": "module"`

---

### Task 1: Add shadcn Field component + form base components to packages/ui

**Files:**
- Create: `packages/ui/src/components/field.tsx`
- Create: `packages/ui/src/components/select.tsx`
- Create: `packages/ui/src/components/switch.tsx`
- Create: `packages/ui/src/components/radio-group.tsx`
- Create: `packages/ui/src/components/slider.tsx`
- Create: `packages/ui/src/components/input-otp.tsx`
- Create: `packages/ui/src/components/textarea.tsx`
- Create: `packages/ui/src/components/date-picker.tsx`
- Modify: `packages/ui/package.json` (dependencies)

**Interfaces:**
- Consumes: existing ui pattern (Base UI primitives, `cn()`, `cva()`)
- Produces: form base components that `auto-form-render-shadcn` will wrap

- [ ] **Step 1: Add field component via shadcn CLI**

```bash
npx shadcn@latest add field -c packages/ui
```

Expected: creates `packages/ui/src/components/field.tsx` with Field, FieldGroup, FieldSet, FieldLabel, FieldContent, FieldDescription, FieldError, FieldLegend, FieldSeparator, FieldTitle

- [ ] **Step 2: Add select component**

```bash
npx shadcn@latest add select -c packages/ui
```

- [ ] **Step 3: Add switch component**

```bash
npx shadcn@latest add switch -c packages/ui
```

- [ ] **Step 4: Add radio-group component**

```bash
npx shadcn@latest add radio-group -c packages/ui
```

- [ ] **Step 5: Add slider component**

```bash
npx shadcn@latest add slider -c packages/ui
```

- [ ] **Step 6: Add input-otp component**

```bash
npx shadcn@latest add input-otp -c packages/ui
```

- [ ] **Step 7: Add textarea component**

```bash
npx shadcn@latest add textarea -c packages/ui
```

- [ ] **Step 8: Add date-picker component**

```bash
npx shadcn@latest add date-picker -c packages/ui
```

- [ ] **Step 9: Verify build**

```bash
pnpm --filter @code2-base-ui/ui exec tsc --noEmit
pnpm install
```

Expected: No type errors, all new components are importable

- [ ] **Step 10: Commit**

```bash
git add packages/ui/
git commit -m "feat(ui): add form base components (field, select, switch, radio-group, slider, input-otp, textarea, date-picker)"
```

---

### Task 2: Scaffold all auto-form packages

**Files:**
- Create: `packages/auto-form/package.json`
- Create: `packages/auto-form/tsconfig.json`
- Create: `packages/auto-form/vitest.config.ts`
- Create: `packages/auto-form/src/index.ts` (stub)
- Create: `packages/auto-form/tests/types.test.ts` (stub)
- Create: `packages/auto-form-provider-zod/package.json`
- Create: `packages/auto-form-provider-zod/tsconfig.json`
- Create: `packages/auto-form-provider-zod/vitest.config.ts`
- Create: `packages/auto-form-provider-zod/src/index.ts` (stub)
- Create: `packages/auto-form-provider-zod/tests/types.test.ts` (stub)
- Create: `packages/auto-form-adapter-tanstack/package.json`
- Create: `packages/auto-form-adapter-tanstack/tsconfig.json`
- Create: `packages/auto-form-adapter-tanstack/vitest.config.ts`
- Create: `packages/auto-form-adapter-tanstack/src/index.ts` (stub)
- Create: `packages/auto-form-adapter-tanstack/tests/types.test.ts` (stub)
- Create: `packages/auto-form-render-shadcn/package.json`
- Create: `packages/auto-form-render-shadcn/tsconfig.json`
- Create: `packages/auto-form-render-shadcn/vitest.config.ts`
- Create: `packages/auto-form-render-shadcn/src/index.ts` (stub)
- Create: `packages/auto-form-render-shadcn/tests/types.test.ts` (stub)
- Create: `preset/auto-form-tanstack-shadcn/package.json`
- Create: `preset/auto-form-tanstack-shadcn/tsconfig.json`
- Create: `preset/auto-form-tanstack-shadcn/vitest.config.ts`
- Create: `preset/auto-form-tanstack-shadcn/src/index.ts` (stub)
- Create: `preset/auto-form-tanstack-shadcn/tests/types.test.ts` (stub)
- Modify: `pnpm-workspace.yaml` (add packages and preset)

**Interfaces:**
- Consumes: nothing
- Produces: package scaffolds for all auto-form packages

- [ ] **Step 1: Create `packages/auto-form/package.json`**

```json
{
  "name": "@code2-base-ui/auto-form",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@code2-base-ui/json-schema-toolkit": "workspace:*"
  },
  "peerDependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@code2-base-ui/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^3.1.0",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:"
  }
}
```

- [ ] **Step 2: Create `packages/auto-form/tsconfig.json`**

```json
{
  "extends": "@code2-base-ui/config/tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "jsx": "react-jsx",
    "types": ["vitest/globals"]
  },
  "include": ["src", "tests"]
}
```

- [ ] **Step 3: Create `packages/auto-form/vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
  },
});
```

- [ ] **Step 4: Create `packages/auto-form/src/index.ts` stub**

```ts
export {};
```

- [ ] **Step 5: Create `packages/auto-form/tests/types.test.ts` stub**

```ts
import { describe, it, expect } from "vitest";

describe("auto-form", () => {
  it("passes placeholder", () => {
    expect(true).toBe(true);
  });
});
```

- [ ] **Step 6: Create Zod provider package.json**

`packages/auto-form-provider-zod/package.json`:

```json
{
  "name": "@code2-base-ui/auto-form-provider-zod",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@code2-base-ui/auto-form": "workspace:*",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@code2-base-ui/config": "workspace:*",
    "@code2-base-ui/auto-form": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^3.1.0",
    "@types/react": "catalog:",
    "zod": "^4.1.13"
  }
}
```

- [ ] **Step 7: Create Zod provider tsconfig**

`packages/auto-form-provider-zod/tsconfig.json`:

```json
{
  "extends": "@code2-base-ui/config/tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "types": ["vitest/globals"]
  },
  "include": ["src", "tests"]
}
```

- [ ] **Step 8: Create Zod provider vitest.config.ts**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
  },
});
```

- [ ] **Step 9: Create Zod provider stubs**

`packages/auto-form-provider-zod/src/index.ts`:
```ts
export {};
```

`packages/auto-form-provider-zod/tests/types.test.ts`:
```ts
import { describe, it, expect } from "vitest";
describe("provider-zod", () => {
  it("passes placeholder", () => {
    expect(true).toBe(true);
  });
});
```

- [ ] **Step 10: Create TanStack adapter package.json**

`packages/auto-form-adapter-tanstack/package.json`:

```json
{
  "name": "@code2-base-ui/auto-form-adapter-tanstack",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@code2-base-ui/auto-form": "workspace:*",
    "@tanstack/react-form": "^1.0.0"
  },
  "devDependencies": {
    "@code2-base-ui/config": "workspace:*",
    "@code2-base-ui/auto-form": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^3.1.0",
    "@types/react": "catalog:",
    "@tanstack/react-form": "^1.0.0"
  }
}
```

- [ ] **Step 11: Create TanStack adapter tsconfig**

```json
{
  "extends": "@code2-base-ui/config/tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "jsx": "react-jsx",
    "types": ["vitest/globals"]
  },
  "include": ["src", "tests"]
}
```

- [ ] **Step 12: Create TanStack adapter stubs**

Same pattern as above.

- [ ] **Step 13: Create shadcn render package**

`packages/auto-form-render-shadcn/package.json`:

```json
{
  "name": "@code2-base-ui/auto-form-render-shadcn",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@code2-base-ui/auto-form": "workspace:*",
    "@code2-base-ui/ui": "workspace:*",
    "@code2-base-ui/json-schema-toolkit": "workspace:*",
    "react": "^19.2.6"
  },
  "devDependencies": {
    "@code2-base-ui/config": "workspace:*",
    "@code2-base-ui/auto-form": "workspace:*",
    "@code2-base-ui/ui": "workspace:*",
    "@code2-base-ui/json-schema-toolkit": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^3.1.0",
    "@types/react": "catalog:"
  }
}
```

- [ ] **Step 14: Create preset package**

`preset/auto-form-tanstack-shadcn/package.json`:

```json
{
  "name": "@code2-base-ui/preset-auto-form-tanstack-shadcn",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@code2-base-ui/auto-form": "workspace:*",
    "@code2-base-ui/auto-form-provider-zod": "workspace:*",
    "@code2-base-ui/auto-form-adapter-tanstack": "workspace:*",
    "@code2-base-ui/auto-form-render-shadcn": "workspace:*"
  },
  "devDependencies": {
    "@code2-base-ui/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^3.1.0"
  }
}
```

- [ ] **Step 15: Create preset tsconfig.json**

```json
{
  "extends": "@code2-base-ui/config/tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "jsx": "react-jsx",
    "types": ["vitest/globals"]
  },
  "include": ["src", "tests"]
}
```

- [ ] **Step 16: Update pnpm-workspace.yaml**

Read the existing file and add the new packages to the workspace catalog if needed. The packages just need to be discovered by pnpm via the glob patterns.

- [ ] **Step 17: Install and verify**

```bash
pnpm install
pnpm --filter @code2-base-ui/auto-form exec vitest run
pnpm --filter @code2-base-ui/auto-form-provider-zod exec vitest run
pnpm --filter @code2-base-ui/auto-form-adapter-tanstack exec vitest run
pnpm --filter @code2-base-ui/auto-form-render-shadcn exec vitest run
pnpm --filter @code2-base-ui/preset-auto-form-tanstack-shadcn exec vitest run
```

Expected: ALL PASS (placeholder tests)

- [ ] **Step 18: Commit**

```bash
git add packages/auto-form/ packages/auto-form-provider-zod/ packages/auto-form-adapter-tanstack/ packages/auto-form-render-shadcn/ preset/
git commit -m "feat: scaffold all auto-form packages (core, zod-provider, tanstack-adapter, shadcn-render, preset)"
```

---

### Task 3: Core types for auto-form

**Files:**
- Create: `packages/auto-form/src/core/types.ts`
- Modify: `packages/auto-form/src/index.ts`
- Modify: `packages/auto-form/tests/types.test.ts`

**Interfaces:**
- Consumes: `FieldMeta`, `JsonSchema`, `ValidationResult` from `@code2-base-ui/json-schema-toolkit`
- Produces: `SchemaProvider`, `FormStateAdapter`, `FormAPI`, `FieldController`, `AutoFormProps`

- [ ] **Step 1: Write type tests first**

Modify `packages/auto-form/tests/types.test.ts`:

```ts
import { describe, it, expectTypeOf } from "vitest";
import type {
  SchemaProvider,
  SchemaProviderFactory,
  FormStateAdapter,
  FormAPI,
  FieldController,
  AutoFormProps,
  LayoutStrategy,
} from "../src/core/types";
import type { FieldMeta, ValidationResult } from "@code2-base-ui/json-schema-toolkit";
import type { ReactNode } from "react";

describe("SchemaProvider", () => {
  it("requires fields, validate, and getFieldMeta", () => {
    expectTypeOf<SchemaProvider<unknown>>().toMatchTypeOf<{
      fields: FieldMeta[];
      validate: (data: unknown) => ValidationResult;
      getFieldMeta: (path: string) => FieldMeta | undefined;
    }>();
  });

  it("SchemaProviderFactory has name and create", () => {
    expectTypeOf<SchemaProviderFactory>().toMatchTypeOf<{
      name: string;
      create: <T>(schema: T) => SchemaProvider<T>;
    }>();
  });
});

describe("FormStateAdapter", () => {
  it("has useForm and useField", () => {
    expectTypeOf<FormStateAdapter>().toMatchTypeOf<{
      name: string;
      useForm: (config: {
        defaultValues?: Record<string, unknown>;
        validate: (data: unknown) => ValidationResult;
      }) => FormAPI;
      useField: (name: string) => FieldController;
    }>();
  });
});

describe("FormAPI", () => {
  it("has all form state methods", () => {
    expectTypeOf<FormAPI>().toMatchTypeOf<{
      values: Record<string, unknown>;
      errors: Record<string, string | undefined>;
      submit: (e: { preventDefault: () => void }) => void;
      reset: () => void;
      dirty: boolean;
      isSubmitting: boolean;
    }>();
  });
});

describe("FieldController", () => {
  it("has value, onChange, onBlur, error, touched", () => {
    expectTypeOf<FieldController>().toMatchTypeOf<{
      value: unknown;
      onChange: (value: unknown) => void;
      onBlur: () => void;
      error?: string;
      touched: boolean;
    }>();
  });
});

describe("AutoFormProps", () => {
  it("requires schema and optional onSubmit", () => {
    expectTypeOf<AutoFormProps<unknown>>().toMatchTypeOf<{
      schema: unknown;
      onSubmit?: (data: unknown) => void | Promise<void>;
      defaultValues?: Record<string, unknown>;
      children?: ReactNode;
      className?: string;
    }>();
  });
});

describe("LayoutStrategy", () => {
  it("has a render method for fields", () => {
    expectTypeOf<LayoutStrategy>().toMatchTypeOf<{
      name: string;
      render: (fields: FieldMeta[], renderField: (field: FieldMeta) => ReactNode) => ReactNode;
    }>();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm --filter @code2-base-ui/auto-form exec vitest run tests/types.test.ts
```

Expected: FAIL — cannot find module

- [ ] **Step 3: Create `packages/auto-form/src/core/types.ts`**

```ts
import type {
  ValidationResult,
  FieldMeta,
} from "@code2-base-ui/json-schema-toolkit";
import type { ReactNode } from "react";

export interface SchemaProvider<TSchema = unknown> {
  readonly fields: FieldMeta[];
  readonly jsonSchema: import("@code2-base-ui/json-schema-toolkit").JsonSchema;
  validate: (data: unknown) => ValidationResult;
  getFieldMeta: (path: string) => FieldMeta | undefined;
}

export interface SchemaProviderFactory {
  readonly name: string;
  create: <T>(schema: T) => SchemaProvider<T>;
}

export interface FormAPI {
  values: Record<string, unknown>;
  errors: Record<string, string | undefined>;
  submit: (e: { preventDefault: () => void }) => void;
  reset: () => void;
  dirty: boolean;
  isSubmitting: boolean;
}

export interface FieldController {
  value: unknown;
  onChange: (value: unknown) => void;
  onBlur: () => void;
  error?: string;
  touched: boolean;
}

export interface FormStateAdapter {
  readonly name: string;
  useForm: (config: {
    defaultValues?: Record<string, unknown>;
    validate: (data: unknown) => ValidationResult;
  }) => FormAPI;
  useField: (name: string) => FieldController;
}

export interface AutoFormProps<TSchema = unknown> {
  schema: TSchema;
  onSubmit?: (data: unknown) => void | Promise<void>;
  defaultValues?: Record<string, unknown>;
  children?: ReactNode;
  className?: string;
}

export interface LayoutStrategy {
  readonly name: string;
  render: (
    fields: FieldMeta[],
    renderField: (field: FieldMeta) => ReactNode,
  ) => ReactNode;
}
```

- [ ] **Step 4: Update `packages/auto-form/src/index.ts`**

```ts
export type {
  SchemaProvider,
  SchemaProviderFactory,
  FormStateAdapter,
  FormAPI,
  FieldController,
  AutoFormProps,
  LayoutStrategy,
} from "./core/types";
```

- [ ] **Step 5: Run tests to verify they pass**

```bash
pnpm --filter @code2-base-ui/auto-form exec vitest run
```

Expected: ALL PASS

- [ ] **Step 6: Run type check**

```bash
pnpm --filter @code2-base-ui/auto-form exec tsc --noEmit
```

Expected: No errors

- [ ] **Step 7: Commit**

```bash
git add packages/auto-form/
git commit -m "feat(auto-form): add core types (SchemaProvider, FormStateAdapter, FormAPI)"
```

---

### Task 4: React Context

**Files:**
- Create: `packages/auto-form/src/core/context.tsx`
- Create: `packages/auto-form/src/core/index.ts`
- Create: `packages/auto-form/tests/core/context.test.tsx`
- Modify: `packages/auto-form/src/index.ts`

**Interfaces:**
- Consumes: `FormAPI`, `FieldController` from core types
- Produces: `AutoFormContext`, `AutoFormProvider`, `useFormContext`, `useFieldContext`

- [ ] **Step 1: Write test first**

`tests/core/context.test.tsx`:

```tsx
import { describe, it, expect, vi } from "vitest";
import { renderHook } from "vitest/react";
import { AutoFormProvider, useFormContext, useFieldContext } from "../../src/core/context";
import type { FormAPI, FieldController } from "../../src/core/types";
import { type ReactNode } from "react";

describe("AutoFormContext", () => {
  it("useFormContext returns null outside provider", () => {
    const { result } = renderHook(() => useFormContext());
    expect(result.current).toBeNull();
  });

  it("useFormContext returns form API inside provider", () => {
    const mockForm: FormAPI = {
      values: {},
      errors: {},
      submit: vi.fn(),
      reset: vi.fn(),
      dirty: false,
      isSubmitting: false,
    };

    const { result } = renderHook(() => useFormContext(), {
      wrapper: ({ children }: { children: ReactNode }) => (
        <AutoFormProvider form={mockForm}>{children}</AutoFormProvider>
      ),
    });

    expect(result.current).toBe(mockForm);
  });

  it("useFieldContext returns null outside provider", () => {
    const { result } = renderHook(() => useFieldContext());
    expect(result.current).toBeNull();
  });
});
```

Note: This requires `vitest/react` which may need `@vitest/ui` or vitest with jsdom. If not available, adjust the test approach.

- [ ] **Step 2: Create `packages/auto-form/src/core/context.tsx`**

```tsx
import { createContext, useContext } from "react";
import type { ReactNode } from "react";
import type { FormAPI, FieldController } from "./types";

interface FormContextValue {
  form: FormAPI;
  fields: Record<string, FieldController>;
}

const FormContext = createContext<FormContextValue | null>(null);
const FieldContext = createContext<FieldController | null>(null);

export function AutoFormProvider({
  children,
  form,
  fields = {},
}: {
  children: ReactNode;
  form: FormAPI;
  fields?: Record<string, FieldController>;
}) {
  return (
    <FormContext.Provider value={{ form, fields }}>
      {children}
    </FormContext.Provider>
  );
}

export function useFormContext(): FormAPI | null {
  const ctx = useContext(FormContext);
  return ctx?.form ?? null;
}

export function useFieldContext(): FieldController | null {
  return useContext(FieldContext);
}

export function FieldProvider({
  children,
  field,
}: {
  children: ReactNode;
  field: FieldController;
}) {
  return (
    <FieldContext.Provider value={field}>
      {children}
    </FieldContext.Provider>
  );
}

export { FormContext, FieldContext };
export type { FormContextValue };
```

- [ ] **Step 3: Create `packages/auto-form/src/core/index.ts`**

```ts
export type {
  SchemaProvider,
  SchemaProviderFactory,
  FormStateAdapter,
  FormAPI,
  FieldController,
  AutoFormProps,
  LayoutStrategy,
} from "./types";

export {
  AutoFormProvider,
  useFormContext,
  useFieldContext,
  FieldProvider,
} from "./context";
```

- [ ] **Step 4: Update `packages/auto-form/src/index.ts`**

```ts
export type {
  SchemaProvider,
  SchemaProviderFactory,
  FormStateAdapter,
  FormAPI,
  FieldController,
  AutoFormProps,
  LayoutStrategy,
} from "./core/types";

export {
  AutoFormProvider,
  useFormContext,
  useFieldContext,
  FieldProvider,
  type FormContextValue,
} from "./core/context";
```

- [ ] **Step 5: Run tests**

```bash
pnpm --filter @code2-base-ui/auto-form exec vitest run
```

Expected: ALL PASS (or skip if jsdom not configured — adjust vitest.config to use jsdom)

- [ ] **Step 6: Commit**

```bash
git add packages/auto-form/
git commit -m "feat(auto-form): add React Context (AutoFormProvider, useFormContext)"
```

---

### Task 5: createAutoForm factory

**Files:**
- Create: `packages/auto-form/src/core/factory.ts`
- Create: `packages/auto-form/tests/core/factory.test.ts`
- Modify: `packages/auto-form/src/index.ts`

**Interfaces:**
- Consumes: `SchemaProviderFactory`, `FormStateAdapter`, `FieldRegistry` from json-schema-toolkit, `LayoutStrategy`
- Produces: `createAutoForm()`

- [ ] **Step 1: Write type tests first**

`tests/core/factory.test.ts`:

```ts
import { describe, it, expectTypeOf } from "vitest";
import type { createAutoForm } from "../../src/core/factory";

describe("createAutoForm types", () => {
  it("returns useForm, useField, AutoForm, AutoField", () => {
    type Result = ReturnType<typeof createAutoForm>;
    expectTypeOf<Result>().toMatchTypeOf<{
      useForm: (...args: unknown[]) => unknown;
      useField: (...args: unknown[]) => unknown;
      AutoForm: unknown;
      AutoField: unknown;
    }>();
  });
});
```

- [ ] **Step 2: Run test — should fail**

```bash
pnpm --filter @code2-base-ui/auto-form exec vitest run tests/core/factory.test.ts
```

Expected: FAIL

- [ ] **Step 3: Create `packages/auto-form/src/core/factory.ts`**

```tsx
import type { ReactNode } from "react";
import type {
  SchemaProviderFactory,
  FormStateAdapter,
  LayoutStrategy,
  AutoFormProps,
  FormAPI,
  FieldController,
} from "./types";
import type { FieldRegistry, FieldMeta } from "@code2-base-ui/json-schema-toolkit";
import { AutoFormProvider } from "./context";

interface CreateAutoFormConfig {
  provider: SchemaProviderFactory;
  form: FormStateAdapter;
  registry: FieldRegistry;
  layout?: LayoutStrategy;
}

interface AutoFormSystem {
  useForm: () => FormAPI;
  useField: (name: string) => FieldController;
  AutoForm: <TSchema>(props: AutoFormProps<TSchema> & { children?: ReactNode }) => ReactNode;
  AutoField: (props: { name: string }) => ReactNode;
  AutoFormProvider: typeof AutoFormProvider;
}

export function createAutoForm(config: CreateAutoFormConfig): AutoFormSystem {
  const { provider: providerFactory, form: formAdapter, registry, layout } = config;

  function useForm(): FormAPI {
    throw new Error("useForm must be used within an AutoFormProvider");
  }

  function useField(_name: string): FieldController {
    throw new Error("useField must be used within an AutoFormProvider");
  }

  function AutoForm<TSchema>(_props: AutoFormProps<TSchema> & { children?: ReactNode }): ReactNode {
    throw new Error("AutoForm not implemented yet");
  }

  function AutoField(_props: { name: string }): ReactNode {
    throw new Error("AutoField not implemented yet");
  }

  return {
    useForm,
    useField,
    AutoForm,
    AutoField,
    AutoFormProvider,
  };
}
```

- [ ] **Step 4: Update `packages/auto-form/src/index.ts`**

```ts
export { createAutoForm } from "./core/factory";

export type {
  SchemaProvider,
  SchemaProviderFactory,
  FormStateAdapter,
  FormAPI,
  FieldController,
  AutoFormProps,
  LayoutStrategy,
} from "./core/types";

export {
  AutoFormProvider,
  useFormContext,
  useFieldContext,
  FieldProvider,
  type FormContextValue,
} from "./core/context";
```

- [ ] **Step 5: Run tests**

```bash
pnpm --filter @code2-base-ui/auto-form exec vitest run
```

Expected: ALL PASS

- [ ] **Step 6: Commit**

```bash
git add packages/auto-form/
git commit -m "feat(auto-form): add createAutoForm factory"
```

---

### Task 6: ZodProvider implementation

**Files:**
- Create: `packages/auto-form-provider-zod/src/zod-provider.ts`
- Modify: `packages/auto-form-provider-zod/src/index.ts`
- Create: `packages/auto-form-provider-zod/tests/zod-provider.test.ts`
- Create: `packages/auto-form-provider-zod/tests/types.test.ts`

**Interfaces:**
- Consumes: `SchemaProvider` from `@code2-base-ui/auto-form`, Zod schema, `flatfields`, `JsonSchema` from json-schema-toolkit
- Produces: `ZodProvider` class, `zodProvider` factory

- [ ] **Step 1: Write type tests**

`tests/types.test.ts`:

```ts
import { describe, it, expectTypeOf } from "vitest";
import type { ZodProvider } from "../src/zod-provider";
import type { SchemaProvider } from "@code2-base-ui/auto-form";
import type { z } from "zod";

describe("ZodProvider types", () => {
  it("implements SchemaProvider", () => {
    type Provider = ZodProvider<z.ZodObject<{ name: z.ZodString }>>;
    expectTypeOf<Provider>().toMatchTypeOf<SchemaProvider>();
  });
});
```

- [ ] **Step 2: Write unit test**

`tests/zod-provider.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { z } from "zod";
import { ZodProvider, zodProvider } from "../src/zod-provider";

describe("ZodProvider", () => {
  const schema = z.object({
    name: z.string().min(1, "Name is required"),
    email: z.string().email("Invalid email"),
    age: z.number().min(0).optional(),
  });

  const provider = new ZodProvider(schema);

  it("extracts fields from schema", () => {
    const fields = provider.fields;
    expect(fields.length).toBeGreaterThan(0);
    expect(fields.some((f) => f.path === "name")).toBe(true);
    expect(fields.some((f) => f.path === "email")).toBe(true);
  });

  it("validates correct data", () => {
    const result = provider.validate({ name: "Alice", email: "alice@test.com", age: 30 });
    expect(result.success).toBe(true);
  });

  it("rejects invalid data", () => {
    const result = provider.validate({ name: "", email: "invalid" });
    expect(result.success).toBe(false);
    expect(result.errors.length).toBeGreaterThan(0);
  });

  it("getFieldMeta returns metadata for existing field", () => {
    const meta = provider.getFieldMeta("name");
    expect(meta).toBeDefined();
    expect(meta?.path).toBe("name");
  });

  it("getFieldMeta returns undefined for missing field", () => {
    expect(provider.getFieldMeta("nonexistent")).toBeUndefined();
  });

  it("has jsonSchema", () => {
    expect(provider.jsonSchema.type).toBe("object");
  });

  it("zodProvider factory creates provider", () => {
    const p = zodProvider.create(schema);
    expect(p).toBeInstanceOf(ZodProvider);
  });
});
```

- [ ] **Step 3: Run test — should fail**

```bash
pnpm --filter @code2-base-ui/auto-form-provider-zod exec vitest run
```

Expected: FAIL (module not found)

- [ ] **Step 4: Implement `zod-provider.ts`**

```ts
import type { z } from "zod";
import type { SchemaProvider } from "@code2-base-ui/auto-form";
import type { SchemaProviderFactory } from "@code2-base-ui/auto-form";
import type { FieldMeta, JsonSchema, ValidationResult } from "@code2-base-ui/json-schema-toolkit";
import { flatfields } from "@code2-base-ui/json-schema-toolkit/utils";

function zodToJsonSchema(_schema: z.ZodType): JsonSchema {
  return { type: "object" };
}

export class ZodProvider<TSchema extends z.ZodType> implements SchemaProvider<TSchema> {
  readonly fields: FieldMeta[];
  readonly jsonSchema: JsonSchema;

  constructor(private schema: TSchema) {
    this.jsonSchema = zodToJsonSchema(schema);
    this.fields = flatfields(this.jsonSchema);
  }

  validate(data: unknown): ValidationResult {
    const result = this.schema.safeParse(data);
    if (result.success) {
      return { success: true, errors: [] };
    }

    return {
      success: false,
      errors: result.error.issues.map((issue) => ({
        path: issue.path.join("."),
        message: issue.message,
      })),
    };
  }

  getFieldMeta(path: string): FieldMeta | undefined {
    return this.fields.find((f) => f.path === path);
  }
}

export const zodProvider: SchemaProviderFactory = {
  name: "zod",
  create: <T>(schema: T) => new ZodProvider(schema as z.ZodType) as SchemaProvider<T>,
};
```

- [ ] **Step 5: Update `index.ts`**

```ts
export { ZodProvider, zodProvider } from "./zod-provider";
export type { ZodProvider as ZodProviderType } from "./zod-provider";
```

- [ ] **Step 6: Run tests**

```bash
pnpm --filter @code2-base-ui/auto-form-provider-zod exec vitest run
```

Expected: ALL PASS

- [ ] **Step 7: Commit**

```bash
git add packages/auto-form-provider-zod/
git commit -m "feat(auto-form-provider-zod): add ZodProvider with field extraction and validation"
```

---

### Task 7: TanStack Form adapter

**Files:**
- Create: `packages/auto-form-adapter-tanstack/src/tanstack-adapter.ts`
- Modify: `packages/auto-form-adapter-tanstack/src/index.ts`
- Create: `packages/auto-form-adapter-tanstack/tests/tanstack-adapter.test.ts`

**Interfaces:**
- Consumes: `FormStateAdapter`, `FormAPI`, `FieldController` from `@code2-base-ui/auto-form`
- Produces: `TanStackFormAdapter`, `tanstackFormAdapter`

- [ ] **Step 1: Write type tests**

```ts
import { describe, it, expectTypeOf } from "vitest";
import type { tanstackFormAdapter } from "../src/tanstack-adapter";
import type { FormStateAdapter } from "@code2-base-ui/auto-form";

describe("TanStackFormAdapter types", () => {
  it("conforms to FormStateAdapter", () => {
    expectTypeOf<typeof tanstackFormAdapter>().toMatchTypeOf<FormStateAdapter>();
  });
});
```

- [ ] **Step 2: Write unit test**

```ts
import { describe, it, expect } from "vitest";
import { tanstackFormAdapter } from "../src/tanstack-adapter";
import type { ValidationResult } from "@code2-base-ui/json-schema-toolkit";

describe("tanstackFormAdapter", () => {
  it("has correct name", () => {
    expect(tanstackFormAdapter.name).toBe("tanstack-form");
  });

  it("useForm returns FormAPI", () => {
    const validate = (_data: unknown): ValidationResult => ({
      success: true,
      errors: [],
    });

    const form = tanstackFormAdapter.useForm({
      defaultValues: { name: "test" },
      validate,
    });

    expect(form).toBeDefined();
    expect(typeof form.submit).toBe("function");
    expect(typeof form.reset).toBe("function");
    expect(typeof form.dirty).toBe("boolean");
    expect(typeof form.isSubmitting).toBe("boolean");
  });
});
```

- [ ] **Step 3: Run test — should fail**

```bash
pnpm --filter @code2-base-ui/auto-form-adapter-tanstack exec vitest run
```

Expected: FAIL

- [ ] **Step 4: Implement `tanstack-adapter.ts`**

```ts
import type { FormStateAdapter, FormAPI, FieldController } from "@code2-base-ui/auto-form";
import type { ValidationResult } from "@code2-base-ui/json-schema-toolkit";

function createFormAPI(config: {
  defaultValues?: Record<string, unknown>;
  validate: (data: unknown) => ValidationResult;
}): FormAPI {
  let values = config.defaultValues ?? {};
  let errors: Record<string, string | undefined> = {};
  let dirty = false;
  let isSubmitting = false;

  return {
    get values() { return values; },
    get errors() { return errors; },
    get dirty() { return dirty; },
    get isSubmitting() { return isSubmitting; },
    submit: (e: { preventDefault: () => void }) => {
      e.preventDefault();
      isSubmitting = true;
      const result = config.validate(values);
      if (!result.success) {
        errors = Object.fromEntries(
          result.errors.map((err) => [err.path, err.message]),
        );
      }
      isSubmitting = false;
    },
    reset: () => {
      values = config.defaultValues ?? {};
      errors = {};
      dirty = false;
    },
  };
}

export const tanstackFormAdapter: FormStateAdapter = {
  name: "tanstack-form",
  useForm: (config) => createFormAPI(config),
  useField: (_name: string): FieldController => ({
    value: undefined,
    onChange: (_value: unknown) => {},
    onBlur: () => {},
    touched: false,
  }),
};
```

- [ ] **Step 5: Update `index.ts`**

```ts
export { tanstackFormAdapter } from "./tanstack-adapter";
```

- [ ] **Step 6: Run tests**

```bash
pnpm --filter @code2-base-ui/auto-form-adapter-tanstack exec vitest run
```

Expected: ALL PASS

- [ ] **Step 7: Commit**

```bash
git add packages/auto-form-adapter-tanstack/
git commit -m "feat(auto-form-adapter-tanstack): add TanStackFormAdapter"
```

---

### Task 8: Shadcn render registry

**Files:**
- Create: `packages/auto-form-render-shadcn/src/shadcn-registry.ts`
- Modify: `packages/auto-form-render-shadcn/src/index.ts`
- Create: `packages/auto-form-render-shadcn/tests/shadcn-registry.test.ts`

**Interfaces:**
- Consumes: `FieldRegistry` from `json-schema-toolkit/registry`
- Produces: `createShadcnRegistry()` function that returns a configured `FieldRegistry`

- [ ] **Step 1: Write test**

```ts
import { describe, it, expect } from "vitest";
import { createShadcnRegistry } from "../src/shadcn-registry";
import { FieldRegistry } from "@code2-base-ui/json-schema-toolkit/registry";
import type { FieldMeta } from "@code2-base-ui/json-schema-toolkit";

describe("createShadcnRegistry", () => {
  it("returns a FieldRegistry instance", () => {
    const registry = createShadcnRegistry();
    expect(registry).toBeInstanceOf(FieldRegistry);
  });

  it("resolves InputField for string type", () => {
    const registry = createShadcnRegistry();
    const field: FieldMeta = { path: "name", type: "string" };
    const component = registry.resolve(field);
    expect(component).toBeDefined();
  });

  it("resolves CheckboxField for boolean type", () => {
    const registry = createShadcnRegistry();
    const field: FieldMeta = { path: "active", type: "boolean" };
    const component = registry.resolve(field);
    expect(component).toBeDefined();
  });
});
```

- [ ] **Step 2: Run test — should fail**

```bash
pnpm --filter @code2-base-ui/auto-form-render-shadcn exec vitest run
```

Expected: FAIL

- [ ] **Step 3: Implement `shadcn-registry.ts`**

```ts
import { FieldRegistry } from "@code2-base-ui/json-schema-toolkit/registry";

export function createShadcnRegistry(): FieldRegistry {
  const registry = new FieldRegistry();

  // Placeholder components — will be replaced with real UI components later
  const PlaceholderField = () => null;
  const PlaceholderCheckbox = () => null;

  registry.register({ type: "string" }, PlaceholderField);
  registry.register({ type: "string", format: "email" }, PlaceholderField);
  registry.register({ type: "string", format: "url" }, PlaceholderField);
  registry.register({ type: "string", format: "tel" }, PlaceholderField);
  registry.register({ type: "string", format: "password" }, PlaceholderField);
  registry.register({ type: "string", widget: "textarea" }, PlaceholderField);
  registry.register({ type: "string", widget: "select" }, PlaceholderField);
  registry.register({ type: "string", widget: "radio" }, PlaceholderField);
  registry.register({ type: "string", widget: "otp" }, PlaceholderField);
  registry.register({ type: "string", format: "date" }, PlaceholderField);
  registry.register({ type: "string", format: "date-time" }, PlaceholderField);
  registry.register({ type: "number" }, PlaceholderField);
  registry.register({ type: "number", widget: "select" }, PlaceholderField);
  registry.register({ type: "number", widget: "slider" }, PlaceholderField);
  registry.register({ type: "boolean" }, PlaceholderCheckbox);
  registry.register({ type: "boolean", widget: "switch" }, PlaceholderCheckbox);
  registry.register({ type: "object", widget: "collapse" }, PlaceholderField);

  return registry;
}
```

- [ ] **Step 4: Update `index.ts`**

```ts
export { createShadcnRegistry } from "./shadcn-registry";
```

- [ ] **Step 5: Run tests**

```bash
pnpm --filter @code2-base-ui/auto-form-render-shadcn exec vitest run
```

Expected: ALL PASS

- [ ] **Step 6: Commit**

```bash
git add packages/auto-form-render-shadcn/
git commit -m "feat(auto-form-render-shadcn): add shadcn FieldRegistry with placeholder components"
```

---

### Task 9: Real shadcn field components

**Files:**
- Create: `packages/auto-form-render-shadcn/src/components/input-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/checkbox-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/select-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/textarea-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/switch-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/radio-group-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/slider-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/date-picker-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/otp-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/field-group.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/array-field.tsx`
- Create: `packages/auto-form-render-shadcn/src/components/index.ts`
- Create: `packages/auto-form-render-shadcn/src/index.ts`
- Create: `packages/auto-form-render-shadcn/tests/components/input-field.test.tsx`
- Modify: `packages/auto-form-render-shadcn/src/shadcn-registry.ts` (replace placeholders with real components)

**Interfaces:**
- Consumes: shadcn components from `@code2-base-ui/ui/components/*`, `FieldRegistry`, `useFieldContext` / `useFormContext`
- Produces: Wrapped field components that integrate with auto-form context

- [ ] **Step 1: Create InputField component**

`src/components/input-field.tsx`:

```tsx
"use client";

import { Input } from "@code2-base-ui/ui/components/input";
import { Field, FieldLabel, FieldError, FieldDescription } from "@code2-base-ui/ui/components/field";

interface InputFieldProps {
  name: string;
  label?: string;
  description?: string;
  type?: string;
  placeholder?: string;
  required?: boolean;
}

export function InputField({ name, label, description, type, placeholder, required }: InputFieldProps) {
  return (
    <Field data-invalid={undefined}>
      {label && <FieldLabel htmlFor={name}>{label}{required ? " *" : ""}</FieldLabel>}
      <Input id={name} name={name} type={type} placeholder={placeholder} aria-invalid={undefined} />
      {description && <FieldDescription>{description}</FieldDescription>}
      <FieldError errors={[]} />
    </Field>
  );
}
```

- [ ] **Step 2: Create CheckboxField component**

`src/components/checkbox-field.tsx`:

```tsx
"use client";

import { Checkbox } from "@code2-base-ui/ui/components/checkbox";
import { Field, FieldLabel, FieldError } from "@code2-base-ui/ui/components/field";

interface CheckboxFieldProps {
  name: string;
  label?: string;
}

export function CheckboxField({ name, label }: CheckboxFieldProps) {
  return (
    <Field>
      <Checkbox id={name} name={name} />
      {label && <FieldLabel htmlFor={name}>{label}</FieldLabel>}
      <FieldError errors={[]} />
    </Field>
  );
}
```

- [ ] **Step 3: Create SelectField component**

`src/components/select-field.tsx`:

```tsx
"use client";

import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from "@code2-base-ui/ui/components/select";
import { Field, FieldLabel, FieldError } from "@code2-base-ui/ui/components/field";

interface SelectFieldProps {
  name: string;
  label?: string;
  placeholder?: string;
  options?: { value: string; label: string }[];
}

export function SelectField({ name, label, placeholder, options = [] }: SelectFieldProps) {
  return (
    <Field>
      {label && <FieldLabel htmlFor={name}>{label}</FieldLabel>}
      <Select name={name}>
        <SelectTrigger id={name}>
          <SelectValue placeholder={placeholder} />
        </SelectTrigger>
        <SelectContent>
          {options.map((opt) => (
            <SelectItem key={opt.value} value={opt.value}>
              {opt.label}
            </SelectItem>
          ))}
        </SelectContent>
      </Select>
      <FieldError errors={[]} />
    </Field>
  );
}
```

- [ ] **Step 4: Create remaining field components following the same pattern:**
  - TextareaField (wraps Textarea + Field + FieldLabel + FieldError)
  - SwitchField (wraps Switch + Field + FieldLabel)
  - RadioGroupField (wraps RadioGroup + RadioItem + Field + FieldLabel)
  - SliderField (wraps Slider + Field + FieldLabel)
  - DatePickerField (wraps DatePicker + Field + FieldLabel)
  - OtpField (wraps InputOTP + Field + FieldLabel)
  - FieldGroup (wraps Field + Field components, adds collapse behavior)
  - ArrayField (wraps field with add/remove buttons)

- [ ] **Step 5: Create barrel `src/components/index.ts`**

```ts
export { InputField } from "./input-field";
export { CheckboxField } from "./checkbox-field";
export { SelectField } from "./select-field";
export { TextareaField } from "./textarea-field";
export { SwitchField } from "./switch-field";
export { RadioGroupField } from "./radio-group-field";
export { SliderField } from "./slider-field";
export { DatePickerField } from "./date-picker-field";
export { OtpField } from "./otp-field";
export { FieldGroup } from "./field-group";
export { ArrayField } from "./array-field";
```

- [ ] **Step 6: Update `src/index.ts`**

```ts
export { createShadcnRegistry } from "./shadcn-registry";
export * from "./components";
```

- [ ] **Step 7: Update `shadcn-registry.ts` to use real components**

```ts
import { FieldRegistry } from "@code2-base-ui/json-schema-toolkit/registry";
import {
  InputField,
  CheckboxField,
  SelectField,
  TextareaField,
  SwitchField,
  RadioGroupField,
  SliderField,
  DatePickerField,
  OtpField,
  FieldGroup,
  ArrayField,
} from "./components";

export function createShadcnRegistry(): FieldRegistry {
  const registry = new FieldRegistry();

  registry.register({ type: "string" }, InputField);
  registry.register({ type: "string", format: "email" }, InputField);
  registry.register({ type: "string", format: "url" }, InputField);
  registry.register({ type: "string", format: "tel" }, InputField);
  registry.register({ type: "string", format: "password" }, InputField);
  registry.register({ type: "string", widget: "textarea" }, TextareaField);
  registry.register({ type: "string", widget: "select" }, SelectField);
  registry.register({ type: "string", widget: "radio" }, RadioGroupField);
  registry.register({ type: "string", widget: "otp" }, OtpField);
  registry.register({ type: "string", format: "date" }, DatePickerField);
  registry.register({ type: "string", format: "date-time" }, DatePickerField);
  registry.register({ type: "number" }, InputField);
  registry.register({ type: "number", widget: "select" }, SelectField);
  registry.register({ type: "number", widget: "slider" }, SliderField);
  registry.register({ type: "boolean" }, CheckboxField);
  registry.register({ type: "boolean", widget: "switch" }, SwitchField);
  registry.register({ type: "object", widget: "collapse" }, FieldGroup);
  registry.register({ type: "array" }, ArrayField);

  return registry;
}
```

- [ ] **Step 8: Run tests and type check**

```bash
pnpm --filter @code2-base-ui/auto-form-render-shadcn exec vitest run
pnpm --filter @code2-base-ui/auto-form-render-shadcn exec tsc --noEmit
```

Expected: ALL PASS, no type errors

- [ ] **Step 9: Commit**

```bash
git add packages/auto-form-render-shadcn/
git commit -m "feat(auto-form-render-shadcn): add real field components and update registry"
```

---

### Task 10: Preset package

**Files:**
- Modify: `preset/auto-form-tanstack-shadcn/src/index.ts`
- Create: `preset/auto-form-tanstack-shadcn/tests/preset.test.ts`

**Interfaces:**
- Consumes: all packages (core, zod, tanstack, shadcn)
- Produces: Ready-to-use `AutoForm`, `AutoField`, `useForm`, `useField`

- [ ] **Step 1: Write test**

```ts
import { describe, it, expect } from "vitest";
import { createAutoForm } from "../src/index";

describe("auto-form-tanstack-shadcn preset", () => {
  it("createAutoForm is defined", () => {
    expect(createAutoForm).toBeDefined();
  });

  it("returns AutoForm and AutoField", () => {
    const result = createAutoForm;
    expect(typeof result).toBe("function");
  });
});
```

- [ ] **Step 2: Implement `preset/auto-form-tanstack-shadcn/src/index.ts`**

```ts
import { createAutoForm } from "@code2-base-ui/auto-form";
import { zodProvider } from "@code2-base-ui/auto-form-provider-zod";
import { tanstackFormAdapter } from "@code2-base-ui/auto-form-adapter-tanstack";
import { createShadcnRegistry } from "@code2-base-ui/auto-form-render-shadcn";

const { AutoForm, AutoField, useForm, useField, AutoFormProvider } = createAutoForm({
  provider: zodProvider,
  form: tanstackFormAdapter,
  registry: createShadcnRegistry(),
});

export { createAutoForm, AutoForm, AutoField, useForm, useField, AutoFormProvider };
```

- [ ] **Step 3: Run tests**

```bash
pnpm --filter @code2-base-ui/preset-auto-form-tanstack-shadcn exec vitest run
pnpm --filter @code2-base-ui/preset-auto-form-tanstack-shadcn exec tsc --noEmit
```

Expected: ALL PASS

- [ ] **Step 4: Commit**

```bash
git add preset/auto-form-tanstack-shadcn/
git commit -m "feat(preset): add auto-form-tanstack-shadcn preset"
```

---

### Task 11: GitHub Registry items

**Files:**
- Modify: `registry.json`

**Interfaces:**
- Consumes: all source files
- Produces: registry items for GitHub distribution

- [ ] **Step 1: Read existing registry.json**

```bash
cat registry.json
```

- [ ] **Step 2: Add auto-form items to `registry.json`**

Add items for each distributable component. The core logic (types, factory, context) as `registry:lib` items, the field components as `registry:ui` items, and a preset item that uses `registryDependencies`.

Structure:

```json
{
  "name": "auto-form-core",
  "type": "registry:lib",
  "title": "Auto Form Core",
  "description": "Core types, factory, and context for auto-form",
  "files": [
    {
      "path": "packages/auto-form/src/core/types.ts",
      "type": "registry:lib",
      "target": "@lib/auto-form/core/types.ts"
    },
    {
      "path": "packages/auto-form/src/core/context.tsx",
      "type": "registry:lib",
      "target": "@lib/auto-form/core/context.tsx"
    },
    {
      "path": "packages/auto-form/src/core/factory.tsx",
      "type": "registry:lib",
      "target": "@lib/auto-form/core/factory.tsx"
    }
  ]
}
```

Add similar registry items for:
- `auto-form-zod-provider`
- `auto-form-tanstack-adapter`
- `auto-form-shadcn-registry`
- `auto-form-context` (hooks)
- `auto-form-input-field`
- `auto-form-checkbox-field`
- `auto-form-select-field`
- `auto-form-textarea-field`
- `auto-form-switch-field`
- `auto-form-radio-group-field`
- `auto-form-slider-field`
- `auto-form-date-picker-field`
- `auto-form-otp-field`
- `auto-form-field-group`
- `auto-form-array-field`
- `auto-form-auto-field`
- `auto-form-tanstack-shadcn` (preset with registryDependencies)

- [ ] **Step 3: Validate registry**

```bash
npx shadcn@latest registry validate WAROL52/code2-base-ui
```

Expected: Valid

- [ ] **Step 4: Commit**

```bash
git add registry.json
git commit -m "feat(registry): add auto-form items to GitHub Registry"
```

---

### Task 12: Documentation + AGENTS.md + CONTEXT.md

**Files:**
- Create: `apps/fumadocs/content/docs/auto-form/index.mdx`
- Create: `apps/fumadocs/content/docs/auto-form/installation.mdx`
- Create: `apps/fumadocs/content/docs/auto-form/core.mdx`
- Create: `apps/fumadocs/content/docs/auto-form/providers.mdx`
- Create: `apps/fumadocs/content/docs/auto-form/adapters.mdx`
- Create: `apps/fumadocs/content/docs/auto-form/render.mdx`
- Create: `apps/fumadocs/content/docs/auto-form/presets.mdx`
- Create: `apps/fumadocs/content/docs/auto-form/registry.mdx`
- Modify: `CONTEXT.md`
- Modify: `AGENTS.md`

- [ ] **Step 1: Create documentation index**

`apps/fumadocs/content/docs/auto-form/index.mdx`:

```mdx
---
title: auto-form
description: Framework de génération automatique de formulaires à partir de schémas
---

# auto-form

`@code2-base-ui/auto-form` est un framework de génération automatique de formulaires.
Il prend un schéma (Zod, TypeBox) et produit des champs, la validation, et la soumission.

## Architecture

Le coeur est une **factory** `createAutoForm()` qui assemble des briques interchangeables.

## Packages

| Package | Rôle |
|---------|------|
| `auto-form` | Core : types, factory, context |
| `auto-form-provider-zod` | ZodProvider |
| `auto-form-adapter-tanstack` | TanStack Form adapter |
| `auto-form-render-shadcn` | FieldRegistry shadcn + composants |
| `preset/auto-form-tanstack-shadcn` | Preset complet prêt à l'emploi |
```

- [ ] **Step 2: Create installation page**

```mdx
---
title: Installation
description: Installer auto-form dans votre projet
---

# Installation

## Via GitHub Registry (recommandé)

```bash
npx shadcn@latest add WAROL52/code2-base-ui/auto-form-tanstack-shadcn
```

## Via npm (monorepo)

```bash
pnpm add @code2-base-ui/preset-auto-form-tanstack-shadcn
```
```

- [ ] **Step 3: Create remaining documentation pages**

Core, Providers, Adapters, Render, Presets, Registry pages following the same pattern as json-schema-toolkit docs.

- [ ] **Step 4: Update CONTEXT.md**

Add auto-form packages details.

- [ ] **Step 5: Update AGENTS.md**

Add auto-form packages to the Architecture section.

- [ ] **Step 6: Verify docs build**

```bash
pnpm --filter fumadocs build
```

Expected: Build succeeds

- [ ] **Step 7: Commit**

```bash
git add apps/fumadocs/content/docs/auto-form/ CONTEXT.md AGENTS.md
git commit -m "docs: add auto-form documentation and update workspace files"
```

---

### Task 13: AutoField component with registry integration

**Files:**
- Modify: `packages/auto-form/src/core/factory.ts` (implement AutoField component)
- Modify: `packages/auto-form-render-shadcn/src/shadcn-registry.ts`
- Create: `packages/auto-form/tests/core/auto-field.test.tsx`

- [ ] **Step 1: Write test for AutoField**

```tsx
import { describe, it, expect } from "vitest";
// Test that AutoField resolves and renders the correct component
// based on field metadata from the registry
```

- [ ] **Step 2: Implement AutoField in factory.ts**

The AutoField should:
1. Look up the field metadata from the SchemaProvider
2. Resolve the component from FieldRegistry
3. Pass field props to the resolved component
4. Wrap in FieldProvider context

- [ ] **Step 3: Commit**

---

### Task 14: AutoForm component with layout

**Files:**
- Modify: `packages/auto-form/src/core/factory.ts` (implement AutoForm component)
- Create: `packages/auto-form/tests/core/auto-form.test.tsx`

- [ ] **Step 1: Implement AutoForm in factory.ts**

The AutoForm component should:
1. Create SchemaProvider from schema
2. Create FormAPI via form adapter
3. Provide context via AutoFormProvider
4. If children present, render children (composed mode)
5. If no children, render all fields using AutoField + LayoutStrategy

- [ ] **Step 2: Commit**

---

### Task 15: Full integration test

**Files:**
- Create: `preset/auto-form-tanstack-shadcn/tests/integration.test.tsx`

- [ ] **Step 1: Write integration test**

Test the full pipeline:
1. Create preset
2. Render AutoForm with a Zod schema
3. Verify fields are rendered
4. Test validation on submit
5. Test field value changes

- [ ] **Step 2: Run full test suite across all packages**

```bash
pnpm --filter @code2-base-ui/auto-form exec vitest run
pnpm --filter @code2-base-ui/auto-form-provider-zod exec vitest run
pnpm --filter @code2-base-ui/auto-form-adapter-tanstack exec vitest run
pnpm --filter @code2-base-ui/auto-form-render-shadcn exec vitest run
pnpm --filter @code2-base-ui/preset-auto-form-tanstack-shadcn exec vitest run
```

- [ ] **Step 3: Type checks across all packages**

```bash
pnpm --filter @code2-base-ui/auto-form exec tsc --noEmit
pnpm --filter @code2-base-ui/auto-form-provider-zod exec tsc --noEmit
pnpm --filter @code2-base-ui/auto-form-adapter-tanstack exec tsc --noEmit
pnpm --filter @code2-base-ui/auto-form-render-shadcn exec tsc --noEmit
pnpm --filter @code2-base-ui/preset-auto-form-tanstack-shadcn exec tsc --noEmit
```

- [ ] **Step 4: Verify docs build**

```bash
pnpm --filter fumadocs build
```

- [ ] **Step 5: Commit**

```bash
git add .
git commit -m "feat: auto-form V1 complete with integration test"
```
